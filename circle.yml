machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t quay.io/pedge/protoeasy .
    - docker build -t quay.io/pedge/protoeasy-proto2 -f Dockerfile.proto2 .
test:
  override:
    - docker run quay.io/pedge/protoeasy example-complete
    - docker run quay.io/pedge/protoeasy-proto2 example-proto2
